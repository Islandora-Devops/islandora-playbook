---

# managed by Ansible

fedora_base_url: {{ milliner_fedora_base_url }}
drupal_base_url: {{ milliner_drupal_base_url }}

db.options:
  driver: {{ milliner_db_options.driver }}
  host: {{ milliner_db_options.host }}
  port: {{ milliner_db_options.port }}
  dbname: {{ milliner_db_options.dbname }}
  user: {{ milliner_db_options.user }}
  password: {{ milliner_db_options.password }}

log:
  # Valid log levels are:
  # DEBUG, INFO, NOTICE, WARNING, ERROR, CRITICAL, ALERT, EMERGENCY, NONE
  # log level none won't open logfile
  level: "{{ milliner_log_level }}"
  file: "{{ milliner_log_file }}"

syn:
  # toggles JWT security for service
  enable: "{{ milliner_jwt_enabled }}"
  # Path to the syn config file for authentication.
  # example can be found here:
  # https://github.com/Islandora-CLAW/Syn/blob/master/conf/syn-settings.example.xml
  config: "{{ milliner_jwt_config }}"